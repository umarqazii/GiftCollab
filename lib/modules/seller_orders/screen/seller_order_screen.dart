import 'package:flutter/material.dart';

class SellerOrderScreenScreen extends StatelessWidget {
  SellerOrderScreenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Seller Orders Tab"),
      ),
    );
  }
}